---
title: merging 汇合成员
sidebar_label: merging 汇合成员
---

队伍菜单要显示全队的总血量。每名队员的血量各是一个 Signal，队员还会随时加入和离开。

[combine](/signal/derive/combine) 的来源在创建时就定死了，最多三个，应付不了人数会变的队伍。自己监听又得在每次有人进队时订阅他的血量、离队时取消订阅，很容易漏掉。

## merging 做什么

`Signals.merging` 同时监听两样东西，成员名单本身，以及名单里每个成员的 Signal。名单变了，或者任何一个成员的 Signal 变了，它都会通知下游。

新成员加入后自动开始监听，离开后自动停止。

它的返回值是 `Signal<Long>`，一个每次变化都会增加的数字。这个数字没有业务含义，只表示「有东西变了」。真正的结果要接一个 `map` 自己算。

## 统计队伍总血量

队伍名单用 [ListSignal](/signal/collection/list) 保存，可以直接增删成员。菜单里显示每名队员，最右边是总血量。

<SignalStepDemo
  code={`
    record Member(String name, MutableSignal<Integer> health) {}

    Member alice = new Member("Alice", Signal.of(20));
    Member bob = new Member("Bob", Signal.of(14));
    Member carol = new Member("Carol", Signal.of(20));

    MutableListSignal<Member> party = ListSignal.of();
    party.addAll(List.of(alice, bob));

    // 第二个参数告诉 merging 怎样从成员取出它的 Signal
    Signal<Long> changed = Signals.merging(party, Member::health);
    Signal<Integer> totalHealth = changed.map(ignored ->
            party.stream().mapToInt(member -> member.health().get()).sum()
    );

    Subscription subscription = totalHealth.onDirty(() -> {
        System.out.println("队伍总血量：" + totalHealth.get());
    });

    System.out.println(totalHealth.get());  // 34
    alice.health().set(15);  // 打印「队伍总血量：29」
    party.remove(bob);       // 打印「队伍总血量：15」
    bob.health().set(0);     // Bob 已经离队，不再监听他，不打印
    party.add(carol);        // 打印「队伍总血量：35」

    subscription.close();
  `}
  title="队伍"
  layout={['ABC#####T']}
  signals={[
    {id: 'party', label: 'party'},
    {id: 'a', label: 'alice.health'},
    {id: 'b', label: 'bob.health'},
    {id: 'total', label: 'totalHealth'},
  ]}
  items={(s) => ({
    A: s.party.includes('Alice') ? {icon: 'golden_apple', name: `Alice ❤ ${s.a}`} : null,
    B: s.party.includes('Bob') ? {icon: 'golden_apple', name: `Bob ❤ ${s.b}`} : null,
    C: s.party.includes('Carol') ? {icon: 'golden_apple', name: 'Carol ❤ 20'} : null,
    T: {icon: 'red_stained_glass_pane', name: `队伍总血量：${s.total}`, nameColor: '#ff5555'},
  })}
  steps={[
    {lines: '1-8', state: {party: 'Alice, Bob', a: 20, b: 14, total: 34}, note: 'Alice 和 Bob 组成队伍，Carol 还没加入。'},
    {lines: '10-14', state: {party: 'Alice, Bob', a: 20, b: 14, total: 34}, note: 'merging 监听名单，以及 Alice、Bob 两人的血量。map 负责把血量加起来。'},
    {lines: '16-18', state: {party: 'Alice, Bob', a: 20, b: 14, total: 34}, note: '订阅总血量。'},
    {lines: '20', state: {party: 'Alice, Bob', a: 20, b: 14, total: 34}, output: '34'},
    {lines: '21', state: {party: 'Alice, Bob', a: 15, b: 14, total: 29}, notified: ['a', 'total'], output: '队伍总血量：29', refresh: ['A', 'T'],
      note: '成员的 Signal 变了。'},
    {lines: '22', state: {party: 'Alice', a: 15, b: 14, total: 15}, notified: ['party', 'total'], output: '队伍总血量：15', refresh: ['B', 'T'],
      note: '名单变了。merging 同时取消了对 Bob 血量的监听。'},
    {lines: '23', state: {party: 'Alice', a: 15, b: 0, total: 15}, notified: ['b'], quiet: ['total'],
      note: 'Bob 已经不在名单里，他的变化与队伍无关。'},
    {lines: '24', state: {party: 'Alice, Carol', a: 15, b: 0, total: 35}, notified: ['party', 'total'], output: '队伍总血量：35', refresh: ['C', 'T'],
      note: 'Carol 加入，merging 开始监听她的血量。'},
  ]}
/>

同样的写法也能算平均血量、存活人数，或者找出血量最低的队员，只要换掉 `map` 里的计算。

## 用普通 Signal 保存名单

成员名单也可以是 `MutableSignal<List<Member>>`。这时要用 `set` 发布一个新列表，比如 `members.set(List.of(alice, carol))`。直接改旧列表不会通知任何人。

`ListSignal` 和 `SetSignal` 能就地增删，也能直接作为成员来源，一般更顺手。

## 注意事项

:::warning[成员与遍历顺序]
取 Signal 的函数应返回成员已有的那个 Signal，不要每次调用都新建一个。成员名单的遍历顺序要稳定。
:::

:::warning[成员里不要放 Player]
名单会被长期持有。成员对象里用 UUID 或名字标识玩家，不要存 `Player`。
:::

<NextStep
  to="/signal/time/debounce"
  title="debounce 防抖"
  description="等输入停下来再处理，例如玩家打完字之后才开始搜索。"
/>
